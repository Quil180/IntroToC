#include <stdio.h>

int main (void) {
  // remember if scanf
  // remember strings and strcpy
  // remember using arrays
  // returning pointers
  // & operater (address)
  // structures and array of structures and with a typedef.
  //  how to build an array of structures
  //  how to access the values in an array of structure
  //  with both arrow and .
  // difference between structure and arrays
  // pointers and values
  // pointer value and pointer address
  // random number generator
  // loop structures
  // a couple of true/falses
  // microcontroller questions
  // how to compare structures
  // use OR for P1DIR and AND to initalize bits
  // index vs elements
  // strlen (length of string is characters plus one [for '\0'])
  return 0;
}
